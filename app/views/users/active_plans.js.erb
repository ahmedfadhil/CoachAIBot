$('document').ready(function() {
    $('#users_plans').empty().html("<%= escape_javascript(render 'users/plans') %>")
    document.getElementById('active_plans_users').style.backgroundColor = '#757575'
});