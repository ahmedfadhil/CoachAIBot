<% if @users.empty? %>
    <br>
    <div id="no-info-grid" class="mdl-grid">
      <div class="mdl-cell--12-col-desktop mdl-cell--8-col-tablet mdl-cell--4-col-phone">
        <div id="no-info-card" class="mdl-card mdl-shadow--4dp">
          <div class="mdl-card__title mdl-card--expand">
            <h2 class="mdl-card__title-text"> Nessuna paziente archivato </h2>
          </div>
          <div id="feature-card-supporting-text" class="mdl-card__supporting-text">

            Non hai ancora archiviato nessun paziente. I pazienti si possono archiviare cliccando sull'icona 'raccoglitore' presente in alto a destra sulla card del paziente.

          </div>
        </div>
      </div>

    </div>
<% end %>

<div class="row">
  <% @users.each do |user|  %>
    <%= cell(:user, user) %>
  <% end %>
</div>
